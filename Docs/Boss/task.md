# ONEngine AI・イベント連動システム 実装仕様書

## 1. データ構造とInterop仕様
*   **AgentIntentComponent (C++ / C# 共通定義)**
    *   **仕様:** Transformを直接操作せず、AI의「意図」を格納するコンポーネント。C#側ではクラス（`class`）として定義する。
    *   **フィールド例:** `Vector3 DesiredMoveDirection`, `bool IsAttacking`, `uint TargetEntityId`
*   **バッチ更新インターフェース**
    *   **仕様:** C++側から毎フレーム1回のみ呼び出されるC#の静的メソッドを定義。

## 2. Behavior Tree (BT) ランタイム仕様
*   **Blackboardの実装**
    *   **仕様:** GCアロケーションをゼロにするため、キーは `uint` (ハッシュ値) を使用。ValueTypeとStringをサポート。
*   **ステートレス BehaviorNode**
    *   **仕様:** ノード自体は状態を持たないフライウェイト。実行状態は `Blackboard` に保存。

## 3. イベントシステム仕様
*   **InvokeEventNode のフェイルセーフ**
    *   **仕様:** 演出待ちイベントのタイムアウト処理によりデッドロックを回避。

## 4. エディター (ImGui) 仕様
*   **ノード自動収集 (Reflection)**
    *   **仕様:** Mono C APIを使用してC#クラスとフィールドを動的に抽出。
*   **デバッグ可視化**
    *   **ノード状態:** 実行中のノードを枠線ハイライト（黄色/緑/赤）で表示。
*   **Undo/Redo**
    *   **仕様:** JSONスナップショットによる最大50ステップの履歴管理。

---

# GeminiCLI向け 実装タスクリスト

### フェーズ1〜5: 基盤構築（完了）
*   [x] Task 1: C++/C# Interop と意図の分離
*   [x] Task 2: Behavior Tree ランタイムの実装
*   [x] Task 3: イベントシステムとフェイルセーフ
*   [x] Task 4: グラフベースエディタ (imgui-node-editor) 統合
*   [x] Task 5: 動作検証と Reactive 化

### フェーズ6〜7: UX強化とUEスタイル再現（ほぼ完了）
*   [x] Task 6-1: ノード・インスペクターの実装
*   [x] Task 6-2: Blackboard Key Selector の導入
*   [x] Task 6-3: リアルタイム実行ビジュアライザー
*   [x] Task 6-4: Decorators の視覚的サポート
*   [x] Task 7-1: モジュール型構造（Decorator/Serviceアタッチ）
*   [x] Task 7-2: イベント駆動通知システム
*   [x] Task 7-3: Observer Aborts 基礎
*   [x] Task 7-4: サービス (Service) ノードの実装
*   [ ] Task 7-5: Blackboard キーの型安全化（フィルタリング）

### フェーズ8: エディタの生産性向上
*   [x] **Task 8-1: ノードのコピー＆ペースト・複製:** 選択したノード（およびその子要素）を `Ctrl+C / Ctrl+V` でコピー＆ペースト、または `Ctrl+D` で複製できる機能を実装する。
*   [ ] **Task 8-2: ノード作成メニューの検索・フィルタリング:** 右クリックメニューに検索窓を追加し、大量のノードクラスから目的のものを素早く見つけられるようにする。
*   [ ] **Task 8-3: コメントボックス（グループ化）:** 複数のノードを囲って名前を付けられる背景ボックス（Sticky Notes）機能を実装し、複雑なツリーの可読性を高める。
*   [ ] **Task 8-4: ズームフィット・選択ノードへのフォーカス:** `F` キーで選択ノードへジャンプ、またはキャンバス全体を画面に収める機能を実装する。

### フェーズ9: 高度なAIロジックのサポート
*   [ ] **Task 9-1: サブツリー (Sub-tree) ノード:** 別のビヘイビアツリー資産を一つのノードとして呼び出せる機能を実装し、AIロジックの再利用性を高める。
*   [ ] **Task 9-2: 標準モジュールライブラリの拡充:** 
    *   Decorator: Cooldown（実行間隔制限）, Loop（繰り返し）, Force Success（強制成功）
    *   Service: Default Focus（ターゲットを自動で向く）
*   [ ] **Task 9-3: ノードの有効/無効切り替え:** 個別のノードを一時的に無効化（スキップ）してテストできる機能を実装する。

### フェーズ10: プロフェッショナルなデバッグ機能
*   [ ] **Task 10-1: 実行時 Blackboard ウォッチャー:** 選択中のエンティティの実メモリ上の Blackboard 変数値をエディタ上でリアルタイムに一覧表示・書き換えできる機能を実装する。
*   [ ] **Task 10-2: ノード・ブレークポイント:** 特定のノードが実行された際にゲームを一時停止（Pause）する機能を実装する。
*   [ ] **Task 10-3: 実行パスの履歴表示 (Breadcrumbs):** 過去数フレームでAIが辿ったパスを薄い線で残し、急な挙動の変化を追跡しやすくする。

### フェーズ11: 品質向上と安定化
*   [ ] **Task 11-1: ツリーの整合性検証 (Validator):** 保存前に「浮いているノード」「循環参照」「ルート未接続」などのエラーを検出し、警告を出す機能を実装する。
*   [ ] **Task 11-2: オートセーブとクラッシュ復旧:** 定期的なバックアップ保存を行い、不測の事態でのデータ喪失を防ぐ。
