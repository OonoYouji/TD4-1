# ONEngine AI・イベント連動システム 実装仕様書

## 1. データ構造とInterop仕様
*   **AgentIntentComponent (C++ / C# 共通定義)**
    *   **仕様:** Transformを直接操作せず、AIの「意図」を格納するコンポーネント。C#側ではクラス（`class`）として定義する。
    *   **フィールド例:** `Vector3 DesiredMoveDirection`, `bool IsAttacking`, `uint TargetEntityId`
    *   **メモリ・同期レイアウト:** C++側（ネイティブ構造体の連続配列）とC#側（コンポーネントクラスのプールされたインスタンス）でデータ構造を一致させ、GCアロケーションを抑えつつシームレスに値を同期・反映できるようにする。
*   **バッチ更新インターフェース**
    *   **仕様:** C++側から毎フレーム1回のみ呼び出されるC#の静的メソッドを定義。
    *   **引数:** `UpdateAI(IntPtr intentsDataPtr, int entityCount, float deltaTime)` のように、連続したメモリブロックのポインタを渡す。C#側ではアンマネージドメモリからデータを読み取り、対応する `AgentIntentComponent` クラスのインスタンス群へ値をバルク更新（または状態の同期）を行う。

## 2. Behavior Tree (BT) ランタイム仕様
*   **Blackboardの実装**
    *   **仕様:** GCアロケーションをゼロにするため、キーは `uint` (ハッシュ値) を使用する。
    *   **データ格納:** ValueType（int, float, bool, Vector3）用の配列 and String用の辞書。
*   **ステートレス BehaviorNode**
    *   **仕様:** ノード自体は状態を持たないシングルトンまたはフライウェイトとして振る舞う。
    *   **状態保存:** ノードごとの実行状態は、自身の `NodeIdHash` をキーとして `Blackboard` に保存・復帰する。

## 3. イベントシステム仕様
*   **Frame Event Queue**
    *   **仕様:** C#側で発行されたイベントを一時的に保持するキュー。AIの評価ループ中は即時実行せず、キューに積むのみ。
*   **InvokeEventNode のフェイルセーフ**
    *   **仕様:** 演出待ちイベントが完了しない場合のデッドロックを回避するタイマー機構。
    *   **処理:** ノード実行開始時にBlackboardへタイムスタンプを記録。毎フレーム `TimeoutSec` と比較し、超過した場合は強制的に `Failure` を返す。

## 4. エディター (ImGui) 仕様
*   **ノード自動収集 (Reflection)**
    *   **仕様:** Mono C APIを使用して、ロードされたC#アセンブリから `BehaviorNode` を継承するクラス一覧と、公開フィールドを抽出し、C++側のImGuiメニューおよびインスペクターを構築する。
*   **デバッグ可視化**
    *   **ノード状態:** C#側からデバッグ用に「直近の実行状態」を毎フレーム受け取り、ImGuiのノードグラフ上で枠線を色分け描画する。
    *   **Blackboard Viewer:** 編集中のBlackboard内変数をImGuiのリストとして表示・編集・保存する。

---

# GeminiCLI向け 実装タスクリスト

### フェーズ1: コア通信とIntentの分離 (C++ / C# Interop)
*   [x] **Task 1-1:** C++側でECSに組み込むネイティブデータとしての `AgentIntentComponent` 構造体を定義し、同時にC#側でもコンポーネントとして扱うためのクラス（`class`）を定義する。
*   [x] **Task 1-2:** C++のメインループからC#のAI更新関数をバッチで呼び出すブリッジ処理を実装する。
*   [x] **Task 1-3:** C++側の `MovementSystem` を修正し、`AgentIntentComponent` の値を読み取って動作するように変更する。

### フェーズ2: Behavior Tree ランタイムの構築
*   [x] **Task 2-1:** ハッシュ値（`uint`）ベースの高速な `Blackboard` クラスをC#で実装する。
*   [x] **Task 2-2:** ステートレスな `BehaviorNode` の基底クラスをC#で実装する。
*   [x] **Task 2-3:** `Sequence` ノードおよび `Selector` ノードを実装する。

### フェーズ3: イベント駆動システムとフェイルセーフ
*   [x] **Task 3-1:** 演出連動のためのイベント発行機能を実装する。
*   [x] **Task 3-2:** AIからイベントを発行する `InvokeEventNode` をC#で実装する。
*   [x] **Task 3-3:** `InvokeEventNode` にフェイルセーフ（タイムアウト）処理を追加する。

### フェーズ4: ツール開発とImGui統合
*   [x] **Task 4-1:** Mono C APIを利用して、C#の `BehaviorNode` 継承クラス一覧を動的に収集するリフレクション処理を実装する。
*   [x] **Task 4-2:** `imgui-node-editor` を利用した、グラフベースのBTエディタをC++で実装する。
*   [x] **Task 4-3:** グラフ構造およびBlackboard変数の JSON 保存・読み込み機能を実装する。

### フェーズ5: 動作検証
*   [x] **Task 5-1:** テストシーンを作成し、プレイヤーを追跡するAIを構築して動作を確認する。
*   [x] **Task 5-2:** 毎フレーム全評価を行う「Reactive Behavior Tree」へとコンポジットノードを刷新する。
*   [x] **Task 5-3:** AI実行前にIntentをリセットすることで、中断時にピタッと止まる挙動を実現する。

### フェーズ6: AIエディタのUX強化（UEスタイル）
*   [x] **Task 6-1: ノード・インスペクター (Node Inspector) の実装:** エディタ上でノードを選択した際、右側にそのノードの公開フィールドを一覧表示し、編集できる機能を実装する。
*   [x] **Task 6-2: Blackboard Key Selector の導入:** `[BlackboardKey]` 属性のフィールドをドロップダウン選択にする機能を実装する。
*   [x] **Task 6-3: リアルタイム実行ビジュアライザー:** 実行中のノードを黄色（Running）や緑（Success）でハイライトする機能を実装する。
*   [x] **Task 6-4: Decorators (条件ノード) の視覚的サポート:** `[Decorator]` 属性を持つノードを青色で表示し、視覚的に区別できるようにする。

### フェーズ7: UEスタイル・ビヘイビアツリーの完全再現
*   [ ] **Task 7-1: モジュール型ノード構造への拡張:** DecoratorやServiceを単立ノードではなく、他のノードにアタッチする「モジュール」として扱うデータ構造とエディタUIを実装する。
*   [ ] **Task 7-2: イベント駆動型実行エンジンの構築:** 毎フレームRootから評価するのをやめ、実行ポインタを維持しつつ、Blackboardの変化によって再評価が走るエンジンへ改修する。
*   [ ] **Task 7-3: Observer Aborts (監視による中断) ロジックの実装:** Decoratorに「Self」「Lower Priority」などの中断スコープを持たせ、条件変化時に実行中のタスクを強制終了（Abort）させる機能を実装する。
*   [ ] **Task 7-4: サービス (Service) ノードの実装:** ノードの実行中に一定間隔で特定の処理（索敵や変数更新）を行う緑色のモジュール機能を実装する。
*   [ ] **Task 7-5: Blackboard キーの型安全化:** Decorator等のプロパティで、期待される型（例: float）と一致するBlackboard変数のみを表示するフィルタリング機能を実装する。
